# API URL (Backend) - MUST include /api/v1 suffix
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# WebSocket URL (for real-time features) - NO /api/v1 suffix
NEXT_PUBLIC_WS_URL=http://localhost:4000

# App Name
NEXT_PUBLIC_APP_NAME=ChatBaby
